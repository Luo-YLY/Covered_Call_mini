# ETF备兑策略研究最终交付验收清单

## 一、交付内容

- 统一看板：组合研究、单ETF周期验证、数据与回测、项目说明。
- 动态研究：用户上传ETF日行情、期权合约、期权日行情后，可选择回测区间并生成单券完整证据。
- 单券参数图谱：每只已确认可用ETF均可生成5档目标Delta × 10档覆盖率，共50个真实回测格点，并展示曲面图和热力图。
- 自选组合：每只ETF独立设置权重、虚值规则和覆盖率；月末固定权重再平衡，周期内按日盯市。
- 冻结研究数据及固化结果仅包含：`159915、510050、510300、510500、588000`。
- 新增ETF上传与动态回测功能完整保留，但本地测试上传包和运行结果不进入交付包。

## 二、启动

在解压后的项目根目录运行：

```powershell
powershell -ExecutionPolicy Bypass -File .\start_dashboard.ps1
```

浏览器打开：`http://127.0.0.1:8765/dashboard/`。

## 三、冻结范围验收

进入看板后，ETF选择器的初始研究数据应仅来自以下五只ETF：

| ETF | 固化单券研究区间 | 交付角色 |
|---|---:|---|
| 159915 | 2022-09-30 至 2026-05-27 | 原始主线 |
| 510050 | 2022-09-30 至 2026-05-27 | 原始主线 |
| 510300 | 2022-09-30 至 2026-05-27 | 原始主线 |
| 510500 | 2022-09-30 至 2026-05-27 | 原始主线 |
| 588000 | 2023-06-30 至 2026-05-27 | 短样本扩展 |

交付包不包含工作区内后来用于测试的ETF数据、`data/user_submissions`、上传样本或动态验收结果。

## 四、新增ETF手动验收

如需继续验证新增ETF能力，在“数据与回测”中输入ETF代码，并依次选择用户自行准备的ETF日行情、期权合约和期权日行情三张CSV。点击“提交并检查数据”后，应显示自动识别的可用日期范围；新增数据写入解压后项目的本地运行目录，不会改写原始五ETF冻结数据。

## 五、完整研究验收

1. 在“数据与回测”选择已上传ETF和可用区间，运行目标Delta 0.30、覆盖率100%的单券回测。
2. 核对净值、回撤、逐期账本、现金流、行权记录、审计和期权当日收盘覆盖率。
3. 点击“确认更新看板”，确认“单ETF周期验证”展示该ETF结果。
4. 前往“组合研究 → 单券参数图谱”，选择该ETF，生成并确认50点图谱；核对排序、三维曲面和热力图。
5. 在“组合研究 → 自选组合”选择需要的原始五ETF，分别设置权重、目标Delta与覆盖率，并在系统显示的共同可用区间内检查日频MTM结果。

## 六、完整性复验

完整项目检查：

```powershell
powershell -ExecutionPolicy Bypass -File .\scripts\verify_delivery.ps1
```

该命令在交付包内会同时校验五ETF白名单，确认标准化行情、期权数据与Delta派生表只含原始五只ETF，并确认本地上传和动态验收目录没有入包。

## 七、数据与研究边界

- 上传端校验项目字段、数据类型、合约关联、日期覆盖和可回测周期，不认证外部数据提供方。
- 日频MTM优先使用所选期权当日收盘；缺失时沿用最近收盘，并在审计中披露覆盖率。
- 单券周期账本采用固定名义本金、逐周期独立结算；组合净值采用月末再平衡、周期内日频MTM。
- 所有结论仅用于研究，不构成收益承诺或交易指令。

## 八、完整性

- `FILE_INDEX.csv`：记录压缩包内每个文件的相对路径、字节数和SHA-256。
- 压缩包旁的 `.sha256`：用于核对整个交付包是否在传输过程中被修改。
- 交付包不包含Token、密码、`.env` 或本机数据接口地址。
