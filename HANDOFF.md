# ETF备兑策略研究最终交接说明

## 一、交付目标

本交接版将原先分散的组合研究与单 ETF 周期验证合并为一个研究产品。对外不再使用内部版本号区分页面，统一从最终研究看板进入。

所有输出仅用于历史研究、验证与展示，不构成交易指令、收益承诺或投资建议。

## 二、统一看板

在解压目录运行：

```powershell
python -m http.server 8765 --bind 127.0.0.1
```

然后访问：

```text
http://127.0.0.1:8765/dashboard/
```

统一看板分为三部分：

1. 组合研究：候选组合、净值、回撤、权重与稳健性证据。
2. 单 ETF 周期验证：权利金、行权或平仓扣减、净期权收益与上涨让渡。
3. 交接说明：研究结论、证据边界与建议阅读顺序。

## 三、当前组合研究结论

当前主线候选为防御收益组合池、回撤预算 20%。主样本为 2022-09-30 至 2026-05-27，共 881 个交易日。

| ETF | 策略规则 | 组合权重 |
|---|---|---:|
| 510300 | 约30天 / 目标Delta40 / 覆盖70% / 持有到期 | 70% |
| 510050 | 约30天 / 目标Delta40 / 覆盖70% / 持有到期 | 14% |
| 159915 | 约30天 / 目标Delta20 / 覆盖10% / 持有到期 | 16% |

历史回测口径下：年化收益 9.799%、Sharpe 0.731、最大回撤 19.96%。这些数值是研究样本结果，不代表未来表现。

## 四、两层证据的区别

- 组合研究使用连续净值、固定权重或回撤预算口径，回答组合层风险收益问题。
- 单 ETF 周期验证按固定名义本金逐期独立结算，回答每期权利金、行权扣减和上涨让渡问题。
- 两层研究的参数可能重合，但统计与会计口径不同，不得把二者当作同一结果的两种展示。

## 五、内部技术目录

以下目录名为历史研究编号，仅用于复现和审计，不再作为交付产品名称：

- `ver3/`：组合研究内部实现。
- `ver4/`：单 ETF 周期验证内部实现。
- `ver2_downside_protection/`：仍被内部实现调用的冻结兼容引擎。
- `outputs/ver3_*`、`outputs/ver4_*`：保留历史编号的研究证据。

在完成依赖迁移、数值对照和全部回归测试之前，不应直接删除或重命名这些内部路径。

## 六、快速验收

```powershell
$env:PYTEST_DISABLE_PLUGIN_AUTOLOAD = "1"
python -m pytest -q tests ver3\tests ver4\tests
```

包内文件：

- `FILE_INDEX.csv`：逐文件路径、字节数及 SHA-256。
- `GIT_SNAPSHOT.txt`：打包时的分支、提交号及工作区状态。
- `RELEASE_README.md`：自动生成的包范围和完整性说明。

## 七、数据与安全边界

- 冻结行情与期权输入随最终交接包提供，但不进入 Git 历史。
- 包内不包含 `.env`、访问令牌、账号密码或本机接口配置。
- 行情和研究数据的分发与使用仍须遵守对应数据授权及内部管理要求。
- 正式跨机器复现前，接收方仍需核对 Python 环境、数据权限并重新运行验收。

## 八、接收方验收步骤

1. 核对 ZIP 与相邻 `.sha256` 文件。
2. 解压后查看 `GIT_SNAPSHOT.txt` 和 `FILE_INDEX.csv`。
3. 启动统一看板，依次查看组合研究、单 ETF 周期验证和交接说明。
4. 运行根目录、组合研究和周期验证测试。
5. 验证冻结输入数据在目标机器上的读取权限。
6. 确认交接通过后，再决定是否删除交付方保留的旧版归档。
