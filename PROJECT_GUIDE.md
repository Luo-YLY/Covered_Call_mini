# ETF备兑策略研究项目说明

## 一、项目目标

本项目把原先分散的组合研究与单 ETF 周期验证合并为一个研究产品。对外不再使用内部版本号区分页面，统一从最终研究看板进入。

所有输出仅用于历史研究、验证与展示，不构成交易指令、收益承诺或投资建议。

## 二、统一看板

在项目根目录运行：

```powershell
.\start_dashboard.ps1
```

然后访问：

```text
http://127.0.0.1:8765/dashboard/
```

统一看板分为四部分：

1. 组合研究：候选组合、净值、回撤、权重与稳健性证据。
2. 单 ETF 周期验证：权利金、行权或平仓扣减、净期权收益与上涨让渡。
3. 数据与回测：提交新的ETF数据、选择回测区间、预览并发布动态结果。
4. 项目说明：研究结论、证据边界与建议阅读顺序。

右上角“查看原始数据”进入本地数据目录页。该页按用途整理行情输入、研究输入和结果底表，所有文件链接均为相对路径。

## 三、新增ETF与动态回测

新增ETF只需要三类通用CSV：ETF日行情、期权合约和期权日行情，数据来源不限，也不要求单独提交ETF元数据。

系统先校验字段和共同样本，再生成标准化行情与 Black-Scholes Delta 派生表。只有能够形成至少12个完整月度周期，并且至少12期可在月末选出DTE20～45且能在下一周期内结算的有效认购期权，才会标记为“完整研究可用”。数据不足仍可保存，补齐后再提交即可。

看板已有ETF可直接选择，无需重复上传，并与新增ETF使用同一选择器。用户可控制开始日期、结束日期，选择目标Delta、ATM或OTM比例，并设置覆盖比例；两类ETF均生成净值、回撤、逐期账本、固定名义本金现金流、滚动12期现金流、路径与市场情景归因及审计记录。回测完成后先显示预览，只有点击“确认更新看板”才会进入“单ETF周期验证”的ETF选择器；其他ETF数据不会被覆盖。单ETF主页也提供同样的直接回测控件，新结果会立即替换整页研究证据。

组合研究的“自选组合”页允许2至8只ETF分别设置权重、虚值规则和覆盖率。系统会取所选ETF的共同可用区间，在月末按固定权重再平衡，并用ETF与所选期权的日收盘生成日频MTM净值；缺失期权报价时沿用最近收盘并披露覆盖率。每个月末MTM必须与周期结算收益严格对账。该自选组合与原日频静态组合主线分开展示，避免把不同样本和策略口径混为一条净值。

## 四、当前组合研究结论

当前主线候选为防御收益组合池、回撤预算 20%。主样本为 2022-09-30 至 2026-05-27，共 881 个交易日。

| ETF | 策略规则 | 组合权重 |
|---|---|---:|
| 510300 | 约30天 / 目标Delta40 / 覆盖70% / 持有到期 | 70% |
| 510050 | 约30天 / 目标Delta40 / 覆盖70% / 持有到期 | 14% |
| 159915 | 约30天 / 目标Delta20 / 覆盖10% / 持有到期 | 16% |

历史回测口径下：年化收益 9.799%、Sharpe 0.731、最大回撤 19.96%。这些数值是研究样本结果，不代表未来表现。

## 五、证据口径

- 组合研究使用连续净值、固定权重或回撤预算口径，回答组合层风险收益问题。
- 单 ETF 周期验证按固定名义本金逐期独立结算，回答每期权利金、行权扣减和上涨让渡问题。
- 用户确认发布的动态回测会成为该ETF在“单ETF周期验证”中的当前自选区间结果，但不会自动替代已验收的组合研究结论。
- 三层研究的参数可能重合，但统计与会计口径不同，不得当作同一结果的不同展示。

## 六、内部技术目录

以下目录名为历史研究编号，仅用于复现和审计，不再作为交付产品名称：

- `ver3/`：组合研究内部实现。
- `ver4/`：单 ETF 周期验证内部实现。
- `ver2_downside_protection/`：仍被内部实现调用的冻结兼容引擎。
- `outputs/ver3_*`、`outputs/ver4_*`：保留历史编号的研究证据。

在完成依赖迁移、数值对照和全部回归测试之前，不应直接删除或重命名这些内部路径。

## 七、快速验收

```powershell
.\scripts\verify_delivery.ps1
```

最终包同时提供 `data/sample_uploads/five_etf_daily/`，包含510050、510300、159919、159915和159922五份完整日频上传样本。五券真实上传、单券回测、50点参数图谱及日频组合的自动验收记录位于 `outputs/final_acceptance/five_etf_acceptance.json`；逐项人工验收见 `DELIVERY_CHECKLIST.md`。

最终包内文件：

- `FILE_INDEX.csv`：逐文件路径、字节数及 SHA-256。
- `GIT_SNAPSHOT.txt`：打包时的分支、提交号及工作区状态。
- `RELEASE_README.md`：自动生成的包范围和完整性说明。

## 八、数据与安全边界

- 冻结行情与期权输入随最终包提供，但不进入 Git 历史。
- 用户提交数据和动态回测结果均保存在本机，默认不进入 Git。
- 包内不包含 `.env`、访问令牌、账号密码或本机接口配置。
- 页面不会访问外部数据服务；系统只做结构和内容校验，不认证上传文件的外部来源。
- 行情和研究数据的分发与使用仍须遵守对应数据授权及内部管理要求。
